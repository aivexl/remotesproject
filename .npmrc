# Ensure all dependencies are installed during production builds
production=false

# Use exact versions for better reproducibility
save-exact=true

# Enable legacy peer deps for better compatibility
legacy-peer-deps=true
